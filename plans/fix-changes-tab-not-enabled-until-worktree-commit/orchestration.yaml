name: fix-changes-tab-not-enabled-until-worktree-commit
description: Fix Changes tab not enabling until worktree commit by fixing git
  diff command and emitting build:files_changed from all file-modifying stages
created: 2026-03-25
compiled: 2026-03-25
mode: errand
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-fix-files-changed
    name: Fix files_changed git command and emit from all file-modifying stages
    depends_on: []
    branch: fix-changes-tab-not-enabled-until-worktree-commit/fix-files-changed
    build:
      - implement
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: errand
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
