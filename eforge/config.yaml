maxConcurrentBuilds: 2
agentRuntimes:
  claude-sdk:
    harness: claude-sdk
defaultAgentRuntime: claude-sdk
build:
  postMergeCommands:
    - pnpm install
    - pnpm build
    - pnpm type-check
    - pnpm test
