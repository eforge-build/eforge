name: keep-eforge-plan-search-index-clean-after-read-only-workstation-refresh
description: Bounded planner compiler artifacts for
  keep-eforge-plan-search-index-clean-after-read-only-workstation-refresh
base_branch: main
validate: []
plans:
  - id: eforge-plan-search-dirty-refresh
    name: Keep eforge-plan search dirty tracking stable across no-op refreshes
    depends_on: []
    branch: keep-eforge-plan-search-index-clean-after-read-only-workstation-refresh/eforge-plan-search-dirty-refresh
    build:
      - implement
      - test-cycle
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
        - test
      maxRounds: 1
      evaluatorStrictness: standard
    test_ownership: builder
pipeline:
  compile:
    - planner
    - planning-quality-review-cycle
  defaultBuild:
    - implement
    - test-cycle
    - review-cycle
  defaultReview:
    strategy: auto
    perspectives:
      - code
      - test
    maxRounds: 1
    evaluatorStrictness: standard
  rationale: >-
    Bounded planner compiler pipeline

    Bounded planner compiler produced final plan artifacts directly.

    Per-plan pipeline settings normalize typed model intent against
    deterministic compiler safety signals.

    Set-level review follows the deepest normalized review; build defaults
    follow the broadest normalized plan pipeline (highest risk score 0).

    - eforge-plan-search-dirty-refresh: no risk factors; declared docs work
    none, test work author-new, test owner builder; model review intent standard
    (The change is localized but correctness-sensitive because dirty tracking
    controls search freshness and rebuild behavior; standard review should
    inspect canonical comparison boundaries and regression assertions.); derived
    build implement -> test-cycle -> review-cycle and auto review with
    perspectives code, test, 1 round(s), standard evaluation
diff_base_ref: 65e5889679775bf5c1f550e56136b520ada71243
