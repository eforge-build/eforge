name: direct-pr-base-sync-recovery-ux
description: Bounded planner compiler artifacts for direct-pr-base-sync-recovery-ux
base_branch: main
validate: []
plans:
  - id: direct-base-sync-budget-config
    name: Direct base-sync budget configuration
    depends_on: []
    branch: direct-pr-base-sync-recovery-ux/direct-base-sync-budget-config
    build:
      - implement
      - doc-sync
      - test-write
      - test-cycle
      - review-cycle
    review:
      strategy: parallel
      perspectives:
        - code
        - security
        - test
        - verify
      maxRounds: 2
      evaluatorStrictness: strict
  - id: direct-base-sync-budget-flow
    name: Direct base-sync fixed budget flow
    depends_on:
      - direct-base-sync-budget-config
    branch: direct-pr-base-sync-recovery-ux/direct-base-sync-budget-flow
    build:
      - implement
      - doc-sync
      - test-write
      - test-cycle
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
        - test
      maxRounds: 1
      evaluatorStrictness: standard
  - id: base-sync-events-contract-cli
    name: Base-sync event contract, CLI rendering, and import discipline
    depends_on:
      - direct-base-sync-budget-flow
    branch: direct-pr-base-sync-recovery-ux/base-sync-events-contract-cli
    build:
      - implement
      - doc-sync
      - test-write
      - test-cycle
      - review-cycle
    review:
      strategy: parallel
      perspectives:
        - code
        - security
        - test
        - verify
      maxRounds: 2
      evaluatorStrictness: strict
  - id: console-direct-base-sync-selectors
    name: Console selector labels for direct base-sync recovery
    depends_on:
      - base-sync-events-contract-cli
    branch: direct-pr-base-sync-recovery-ux/console-direct-base-sync-selectors
    build:
      - implement
      - test-write
      - test-cycle
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
        - test
      maxRounds: 1
      evaluatorStrictness: standard
  - id: console-direct-base-sync-lanes
    name: Pipeline lane labels for base-sync and merge-resolver activity
    depends_on:
      - console-direct-base-sync-selectors
    branch: direct-pr-base-sync-recovery-ux/console-direct-base-sync-lanes
    build:
      - implement
      - test-write
      - test-cycle
      - review-cycle
    review:
      strategy: single
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
pipeline:
  compile:
    - planner
    - planning-quality-review-cycle
  defaultBuild:
    - implement
    - test-cycle
    - review-cycle
  defaultReview:
    strategy: parallel
    perspectives:
      - code
      - security
      - test
      - verify
    maxRounds: 2
    evaluatorStrictness: strict
  rationale: >-
    Bounded planner compiler pipeline

    Bounded planner compiler produced final plan artifacts directly.

    Per-plan pipeline settings derived deterministically from compiler risk
    signals.

    Set-level defaults follow the highest plan risk score (3).

    - base-sync-events-contract-cli: risk score 3 (large-plan, multi-subsystem,
    dependency-root); declared docs work sync-existing, test work author-new;
    derived build implement -> doc-sync -> test-write -> test-cycle ->
    review-cycle and parallel review with perspectives code, security, test,
    verify, 2 round(s), strict evaluation

    - console-direct-base-sync-lanes: no risk factors; declared docs work none,
    test work author-new; derived build implement -> test-write -> test-cycle ->
    review-cycle and single review with perspectives code, 1 round(s), standard
    evaluation

    - console-direct-base-sync-selectors: risk score 1 (dependency-root);
    declared docs work none, test work author-new; derived build implement ->
    test-write -> test-cycle -> review-cycle and auto review with perspectives
    code, test, 1 round(s), standard evaluation

    - direct-base-sync-budget-config: risk score 3 (large-plan,
    low-confidence-localization, dependency-root); declared docs work
    sync-existing, test work author-new; derived build implement -> doc-sync ->
    test-write -> test-cycle -> review-cycle and parallel review with
    perspectives code, security, test, verify, 2 round(s), strict evaluation

    - direct-base-sync-budget-flow: risk score 1 (dependency-root); declared
    docs work sync-existing, test work author-new; derived build implement ->
    doc-sync -> test-write -> test-cycle -> review-cycle and auto review with
    perspectives code, test, 1 round(s), standard evaluation
diff_base_ref: 0e647c5ddb1985bd8ff4d06e108cefc91c23a15f
