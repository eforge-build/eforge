name: add-eforge-config-init-cli-command
description: Add eforge config init CLI command for interactive config
  scaffolding with documentation updates
created: 2026-03-29
compiled: 2026-03-29
mode: cli-feature
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
  - pnpm build
plans:
  - id: plan-01-config-init
    name: Add eforge config init CLI command
    depends_on: []
    branch: add-eforge-config-init-cli-command/config-init
    build:
      - - implement
        - doc-update
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: excursion
  description: Multi-file feature work or refactors. Use when the full scope can
    be planned in a single planner session - all plans enumerated, all file
    changes listed, and cross-plan dependencies resolved. Covers tightly coupled
    changes (type cascades, interface refactors, rename-and-update-all-callers)
    and any work where one planning pass is sufficient.
  compile:
    - planner
    - plan-review-cycle
