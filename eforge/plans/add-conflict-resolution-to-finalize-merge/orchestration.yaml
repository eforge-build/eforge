name: add-conflict-resolution-to-finalize-merge
description: Add MergeResolver conflict resolution to the finalize merge step so
  resolvable conflicts on the base branch don't fail an otherwise successful
  build
created: 2026-03-29
compiled: 2026-03-29
mode: targeted-fix
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-finalize-merge-resolver
    name: Add conflict resolution to finalize merge
    depends_on: []
    branch: add-conflict-resolution-to-finalize-merge/finalize-merge-resolver
    build:
      - implement
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: errand
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
