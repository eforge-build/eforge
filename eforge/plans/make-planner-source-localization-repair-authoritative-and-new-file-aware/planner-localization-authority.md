---
id: planner-localization-authority
name: Make planner source localization repair authoritative and new-file aware
branch: make-planner-source-localization-repair-authoritative-and-new-file-aware/planner-localization-authority
---

# Make planner source localization repair authoritative and new-file aware

Own the planner-compiler trust boundary across `packages/engine/src/planner-compiler/reduce-contracts.ts`, `packages/engine/src/planner-compiler/source-localization.ts`, `packages/engine/src/planner-compiler/source-localization-repair.ts`, `packages/engine/src/planner-compiler/exploration-contracts.ts`, `packages/engine/src/planner-compiler/adaptive-rescope.ts`, `packages/engine/src/planner-compiler/shared-brief.ts`, `packages/engine/src/planner-compiler/source-evidence-materialization.ts`, and their direct reducer-task and compile-orchestration callers. Introduce domain-correct bounded id validation and diagnostics; preserve fallback localization; constrain new-file ownership to explicit in-root intent in the active checkout/worktree; keep ambiguous existing-behavior edits fail-closed; make unresolved critical/representation owners authoritative for exploration; prioritize exact, criterion-linked, and repair evidence; materialize repair evidence and selectively rerun affected atoms. Author focused tests and the root-cause-reporting integration fixture in the same module because they validate inseparable compiler contracts. Exclude release/configuration docs and generic `shared`, `general`, or eforge-plan test candidates, which are lexical localization noise.

## Traceability

Criteria: ac-001, ac-002, ac-003, ac-004, ac-005, ac-006, ac-007, ac-008, ac-009, ac-010, ac-011, ac-012, ac-013
Aspects: ac-001:general:general, ac-002:subsystem:aspect-based, ac-002:subsystem:criterion, ac-003:general:general, ac-004:general:general, ac-005:general:general, ac-006:general:general, ac-007:general:general, ac-008:subsystem:lexical, ac-008:subsystem:shared, ac-009:general:general, ac-010:interface:test, ac-010:subsystem:test, ac-011:general:general, ac-012:interface:test, ac-012:subsystem:test, ac-013:interface:test, ac-013:subsystem:test

## Validation

Focused Vitest coverage must reject cross-domain, unknown, and out-of-node reducer ids without suppressing fallback; prove bounded in-root new-file ownership and traversal/ambiguity rejection; force exploration for unresolved critical/representation owners; prove targeted repair attempt/tool/file/byte bounds, selective reruns, priority under evidence contention, and subsystem isolation; and compile the modeled integration fixture into buildable artifacts without the two owner-localization failures. Then run existing bounded planner/adaptive-rescope/source-evidence/compile-stage suites plus `pnpm type-check`, `pnpm test`, and `pnpm maintainability:check`.

## Fragment: Authoritative planner localization and repair

Implement one cohesive planner-compiler hardening change.

1. In `packages/engine/src/planner-compiler/reduce-contracts.ts` and its direct reducer task/prompt construction callers, expose the node's bounded valid source-need and affected-atom catalogs to reducer tasks. Validate structured submissions against the correct catalog, diagnosing or deterministically quarantining unknown source-need ids and unknown or out-of-node affected-atom ids. Preserve separate meanings for `sourceIds`, `sourceNeedIds`, and `affectedAtomIds`: keep `sourceIds` as provenance only, and never reinterpret source, atom, or reducer ids across domains.
2. In `packages/engine/src/planner-compiler/source-localization.ts` and `packages/engine/src/planner-compiler/source-localization-repair.ts`, filter and diagnose unknown explicit source-need ids before deciding whether an explicit set exists; if none remain, continue criterion/aspect fallback. Scope generic subsystem matching so labels such as `general`, `shared`, `lexical`, `criterion`, or `test` cannot broadcast unrelated literal paths or directories to other atoms.
3. Add a conservative proposed-ownership path for focused new-file intent: accept a nonexistent target only when explicit PRD paths or affected-subsystem evidence identify a bounded path beneath a verified existing repository directory for the active checkout/worktree. Keep ambiguous changes to existing behavior fail-closed, reject traversal, out-of-root, and broad-root-only proposals, and do not weaken normal existing-file ownership checks.
4. In `packages/engine/src/planner-compiler/exploration-contracts.ts`, `packages/engine/src/planner-compiler/adaptive-rescope.ts`, and direct compile orchestration callers, gate exploration on every unresolved critical or representation-required implementation owner rather than aggregate literal-path confidence. In `packages/engine/src/planner-compiler/source-localization-repair.ts`, derive targeted queries from affected criteria, aspects, interfaces, subsystem hints, and explicit source roots. Enforce existing explicit attempt, tool-use, file, and byte ceilings rather than increasing retry counts; materialize newly returned evidence through `packages/engine/src/planner-compiler/source-evidence-materialization.ts`; and rerun only affected planning atoms while preserving unaffected results.
5. In `packages/engine/src/planner-compiler/shared-brief.ts`, `packages/engine/src/planner-compiler/source-localization.ts`, and `packages/engine/src/planner-compiler/source-evidence-materialization.ts`, make criterion linkage and exact paths outrank generic subsystem/interface overlap. Reserve materialization order for explicit PRD paths, affected-criterion candidates, and repair-priority paths before global lexical/shared evidence, preserving deterministic ordering and all existing budget ceilings.
6. Add cohesive focused tests (a new planner-localization repair logical-unit test file is appropriate) for invalid/cross-domain/out-of-node ids, fallback despite bad explicit ids, safe new-file ownership in an active worktree, traversal and ambiguity rejection, critical-owner gating, bounded targeted repair/selective rerun, evidence ordering, and subsystem isolation. Add an integration fixture modeled on `add-upstream-plan-root-cause-reporting` and assert compilation yields buildable plan artifacts without `classifier-owner-path-unlocalized` or `no localized owner paths resolved`. Keep `test/planning-aspect-coverage.test.ts`, `test/planning-shared-brief-scale.test.ts`, and the existing bounded compiler, adaptive-rescope, source-evidence, and compile-stage suites green. Follow the repository policy: test real pure functions, use I/O fixtures only where required, and do not add mocks.
7. Validate with focused Vitest runs, then `pnpm type-check`, `pnpm test`, and `pnpm maintainability:check`. Do not modify release skills, workflows, changelog, unrelated recovery runtime files, client event modules unless a cross-process planning diagnostic truly changes, or unrelated generic-shared packages; the compiler-localized candidates for those areas are test/keyword noise rather than implementation owners.

## Execution Intent

Test ownership: builder
Review depth: heavy
Review rationale: risk score 1 (low-confidence-localization); declared docs work none, test work author-new, test owner builder; model review intent heavy (This changes compiler trust boundaries, path ownership under worktrees, evidence-budget ordering, and selective rerun scheduling; subtle fail-open behavior could produce unbuildable plans while overcorrection could force unnecessary exploration.); derived build implement -> test-cycle -> review-cycle and parallel review with perspectives code, security, test, verify, 2 round(s), strict evaluation