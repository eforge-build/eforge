# Deleted Test Coverage Evidence

Created before test deletions/consolidations for plan-01-thin-test-suite. Test commands were not run in this builder stage per orchestration instructions; validation evidence below uses retained static coverage plus `pnpm build` and `pnpm type-check`.

| Area | Deleted/consolidated test | File | Retained coverage | Validation/evidence | Notes |
| --- | --- | --- | --- | --- | --- |
| Client events | Deleted file/describe `EforgeEvent wire roundtrip` (`roundtrips all 20 new daemon variants through JSON`; `covers all 20 new daemon variant type literals`) | `packages/client/src/__tests__/events.test.ts` | `events-wire-parity-valid-fixtures.ts` contains daemon lifecycle, heartbeat, scheduler, auto-build, recovery, orphan, warning, and error fixtures; `events-wire-parity.test.ts` validates fixtures through `safeParseEforgeEvent`; invalid shapes remain in `events-wire-parity-invalid.test.ts`. | Static evidence: `rg "daemon:lifecycle:starting\\|daemon:error" packages/client/src/__tests__/events-wire-parity-valid-fixtures.ts`; verification: `pnpm build`, `pnpm type-check`. | Deleted stale JSON-only duplicate file. |
| Client events | Deleted describe `new plan lifecycle + merge-worktree variants — JSON roundtrip` (`roundtrips all 5 new variant types through JSON`; `covers all 5 new variant type literals`) | `packages/client/src/__tests__/events-schemas.test.ts` | Focused `safeParseEforgeEvent` tests in the same file still cover all plan status values, plan error set/clear, merge worktree set/clear, optional envelope fields, daemon heartbeat, and registry metadata; wire parity fixtures also include plan lifecycle and merge-worktree variants. | Static evidence: `rg "plan:status:change\\|merge:worktree:clear" packages/client/src/__tests__/events-wire-parity-valid-fixtures.ts packages/client/src/__tests__/events-schemas.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Removed now-unused `newVariants` and `NEW_VARIANT_TYPES` helper exports. |
| Console selectors | Consolidated `slugToDisplayLabel` one-assertion cases: hyphen words, underscore words, PRD/UI/MCP/CLI/API acronyms, ISO date prefix, `.md` extension, single word, multiple acronyms | `packages/console-ui/src/__tests__/labels.test.ts` | Labeled `it.each` preserves every input/output pair with labels such as `slug acronym: mcp` and `date-prefix: ISO`. | Static evidence: table entries in `labels.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Table labels identify the lost behavior on failure. |
| Console selectors | Consolidated `selectPrdDisplayLabel` explicit-title cases: clean title, trim whitespace, title matching slug | `packages/console-ui/src/__tests__/labels.test.ts` | Labeled explicit-title `it.each` retains all expected outputs. | Static evidence: explicit-title table in `labels.test.ts`; verification: `pnpm build`, `pnpm type-check`. | No behavior deleted. |
| Console selectors | Consolidated markdown-title rejection cases: `#`, `##`, bold, backtick, markdown link, newline body leak, overly long body leak | `packages/console-ui/src/__tests__/labels.test.ts` | Labeled markdown rejection table retains slug fallback outputs, including `markdown rejection: newline body leak`. | Static evidence: markdown-title rejection table in `labels.test.ts`; verification: `pnpm build`, `pnpm type-check`. | No behavior deleted. |
| Console selectors | Consolidated slug fallback cases: undefined, null, empty, whitespace-only title, CLI slug acronym, raw id fallback | `packages/console-ui/src/__tests__/labels.test.ts` | Labeled fallback table retains all expected slug-derived outputs. | Static evidence: slug title-casing fallback table in `labels.test.ts`; verification: `pnpm build`, `pnpm type-check`. | No behavior deleted. |
| Console selectors | Consolidated `selectValidateTarget` path/name/fallback tests | `packages/console-ui/src/views/system/__tests__/extension-management-selectors.test.ts` | Labeled table covers project-local, project-team, user named, external named, and external unnamed behavior. | Static evidence: `validate target:*` table entries; verification: `pnpm build`, `pnpm type-check`. | No behavior deleted. |
| Console selectors | Consolidated action eligibility tests for validate/trust/re-trust/untrust/promote/demote | `packages/console-ui/src/views/system/__tests__/extension-management-selectors.test.ts` | Labeled tables retain project-local, project-team, user, external named/unnamed, legacy trust, rich trustState, changed/trusted/untrusted, promote, and demote cases. | Static evidence: `validate eligibility:*`, `trust eligibility:*`, `re-trust eligibility:*`, `untrust eligibility:*`, `promote eligibility:*`, and `demote eligibility:*` table entries; verification: `pnpm build`, `pnpm type-check`. | `canReloadExtensions` remains a standalone assertion. |
| Console selectors | Consolidated `selectTrustAction` changed/untrusted/trusted/user cases | `packages/console-ui/src/views/system/__tests__/extension-management-selectors.test.ts` | Labeled trust-action table retains `re-trust`, `trust`, and `null` outcomes. | Static evidence: `trust action:*` table entries; verification: `pnpm build`, `pnpm type-check`. | No behavior deleted. |
| Console selectors | Consolidated `hasAnyExtensionAction` project-team/project-local/user/external unnamed cases | `packages/console-ui/src/views/system/__tests__/extension-management-selectors.test.ts` | Labeled action-availability table retains all true/false outcomes. | Static evidence: `action availability:*` table entries; verification: `pnpm build`, `pnpm type-check`. | No behavior deleted. |
| Console selectors | Consolidated label/copy selector checks for mutation labels, pending labels, titles, consequences, and unavailable reasons | `packages/console-ui/src/views/system/__tests__/extension-management-selectors.test.ts` | Labeled copy tables retain trust/re-trust/untrust/promote/demote labels, pending labels, title substrings, consequence regexes, promote non-trust assertion, and unavailable-reason regexes. | Static evidence: label/copy tables in `extension-management-selectors.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Promote negative assertion remains standalone for clarity. |
| Monitor static UI | Converted combined direct-helper rejection test to labeled table | `packages/monitor/src/__tests__/http-static-assets.test.ts` | Direct helper coverage still includes malformed percent escape, encoded traversal, multiple leading slash rejection, and encoded slash rejection; symlink escape remains `skipIf(!symlinksAvailable)`. | Static evidence: `rg "malformed percent\\|encoded traversal\\|symlink\\|asset misses" packages/monitor/src/__tests__/http-static-assets.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Labels remain specific. |
| Monitor static UI | Deleted full-server duplicate `returns 404 for a missing Console asset` | `packages/monitor/src/__tests__/static-ui-serving.test.ts` | `http-static-assets.test.ts` retains direct helper `returns 404 for Console asset misses`; full-server tests still cover root redirects, `/console/`, assets, SPA fallback, and API 404 route wiring. | Static evidence: `rg "asset misses\\|/api/not-a-route\\|serves Console assets" packages/monitor/src/__tests__`; verification: `pnpm build`, `pnpm type-check`. | Low-level duplicate removed only from full-server suite. |
| Monitor static UI | Deleted full-server duplicate `returns 400 for malformed percent escapes under /console` | `packages/monitor/src/__tests__/static-ui-serving.test.ts` | `http-static-assets.test.ts` retains labeled direct helper row `malformed percent escape`. | Static evidence: `rg "malformed percent" packages/monitor/src/__tests__/http-static-assets.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Security-adjacent behavior retained at helper layer. |
| Monitor static UI | Deleted full-server duplicate `rejects encoded traversal under /console assets` | `packages/monitor/src/__tests__/static-ui-serving.test.ts` | `http-static-assets.test.ts` retains labeled direct helper row `encoded traversal`. | Static evidence: `rg "encoded traversal" packages/monitor/src/__tests__/http-static-assets.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Security-adjacent behavior retained at helper layer. |
| Monitor static UI | Deleted full-server duplicate `rejects symlink escapes from the Console root` | `packages/monitor/src/__tests__/static-ui-serving.test.ts` | `http-static-assets.test.ts` retains `it.skipIf(!symlinksAvailable)('rejects symlink escapes from the Console root', ...)`. | Static evidence: `rg "symlink escapes" packages/monitor/src/__tests__/http-static-assets.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Removed full-server symlink setup/imports. |
| Engine provenance | Consolidated 12 `parseGitHubRepoFromRemote` one-assertion cases into a descriptive table | `test/provenance.test.ts` | Table retains HTTPS with/without `.git`, `git+https`, scp-like SSH with/without `.git`, `ssh://` with/without `.git`, GitLab, Bitbucket, local file path, empty string, and hyphenated owner/repo behavior. | Static evidence: parser `it.each` entries in `test/provenance.test.ts`; verification: `pnpm build`, `pnpm type-check`. | No parser behavior deleted. |
| Engine provenance | Deleted real-git collector integration `renders GitHub blob URL for git+https remote` | `test/provenance.test.ts` | Parser table retains `git+https` remote parsing; retained HTTPS collector integration still proves `collectBuildArtifactProvenance` creates GitHub blob URLs from a real repository. | Static evidence: `rg "git\+https remote\\|renders GitHub blob URL for HTTPS" test/provenance.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Integration duplicate removed. |
| Engine provenance | Deleted real-git collector integration `renders GitHub blob URL for scp-like SSH remote (git@github.com:...)` | `test/provenance.test.ts` | Parser table retains scp-like SSH with and without `.git`; retained HTTPS collector integration still proves blob URL construction from a real repository. | Static evidence: `rg "scp-like SSH\\|renders GitHub blob URL for HTTPS" test/provenance.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Integration duplicate removed. |
| Engine provenance | Deleted real-git collector integration `renders GitHub blob URL for ssh:// remote` | `test/provenance.test.ts` | Parser table retains `ssh://` with and without `.git`; retained HTTPS collector integration still proves blob URL construction from a real repository. | Static evidence: `rg "ssh:// remote\\|renders GitHub blob URL for HTTPS" test/provenance.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Integration duplicate removed. |
| First-party extension packages | Deleted duplicate `declares publishable first-party package metadata and release wiring` | `eforge/extensions/eforge-plan/__tests__/package-publication.test.ts` | `package-foundation.test.ts` retains metadata, exports, files, dependencies, workspace, type-check script, and lockstep release wiring coverage; npm pack contents test retains public publish file shape. | Static evidence: `rg "declares public package metadata\\|participates in workspace\\|packs only compiled" eforge/extensions/eforge-plan/__tests__/package-*.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Duplicate publication metadata assertions removed. |
| First-party extension packages | Deleted duplicate `builds required runtime and workstation assets before packing` | `eforge/extensions/eforge-plan/__tests__/package-publication.test.ts` | `beforeAll(ensureBuilt)` still builds before publication tests; `package-foundation.test.ts` retains `builds import-safe runtime and workstation artifacts`; npm pack contents test asserts built file list. | Static evidence: `rg "ensureBuilt\\|builds import-safe runtime\\|packs only compiled" eforge/extensions/eforge-plan/__tests__/package-*.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Build-file existence duplicate removed. |
| First-party extension packages | Keep rationale: retained `builds import-safe runtime and workstation artifacts` | `eforge/extensions/eforge-plan/__tests__/package-foundation.test.ts` | Unique public-package confidence: imports built runtime/workstation artifacts and verifies registered actions, input sources, deep links, integration commands, and console workstations. | Static evidence: test body imports `dist/index.js` and uses `createExtensionRecorder`; verification: `pnpm build`, `pnpm type-check`. | Expensive test intentionally kept. |
| First-party extension packages | Keep rationale: retained `packs only compiled runtime, workstation assets, and package metadata` | `eforge/extensions/eforge-plan/__tests__/package-publication.test.ts` | Unique npm pack confidence: validates dry-run file list includes prompts/built assets and excludes source, tests, node_modules, tsconfig, and tsup config. | Static evidence: `npmPackDryRunFiles()` and package file assertions; verification: `pnpm build`, `pnpm type-check`. | Expensive test intentionally kept. |
| First-party extension packages | Keep rationale: retained `keeps compiled runtime self-contained for fresh-project imports` | `eforge/extensions/eforge-plan/__tests__/package-publication.test.ts` | Unique fresh-project import safety: rejects repository source paths and first-party `@eforge-build/*` imports in built JS. | Static evidence: `runtimeImportSpecifiers()` and `isAllowedRuntimeImport()` checks; verification: `pnpm build`, `pnpm type-check`. | Expensive test intentionally kept. |
| First-party extension packages | Keep rationale: retained `eforge-playbooks package publication` combined build/import/npm-pack test | `eforge/extensions/eforge-playbooks/__tests__/package-publication.test.ts` | Unique public artifact confidence for eforge-playbooks: builds if needed, imports built entrypoint, and verifies npm pack includes only public files. | Static evidence: test imports `dist/index.js` and runs `npm pack --dry-run --json`; verification: `pnpm build`, `pnpm type-check`. | Left in place because retained coverage was not an unambiguous duplicate. |
| First-party extension packages | Keep rationale: retained eforge-playbooks registration and planning contract tests | `eforge/extensions/eforge-playbooks/__tests__/registration.test.ts`; `eforge/extensions/eforge-playbooks/__tests__/planning-contract.test.ts` | Registration proves action IDs, commands, console contribution, capabilities, optional dependency metadata; planning contract proves eforge-plan handoff capability/action/command/deep link/workstation IDs. | Static evidence: registration and planning contract assertions; verification: `pnpm build`, `pnpm type-check`. | Adjacent confidence retained without npm pack duplication. |
| Extension package-management routes | Deleted brittle source-string inspection `POST extensionUpdate applies version overrides only to npm sidecar sources` | `test/extension-tooling-routes-package-management.test.ts` | Public behavior tests remain for persisted effective npm source spec, npm `file:` sidecar rejection, invalid registry version override rejection, path sidecar rejection, tarball sidecar rejection, and `DAEMON_API_VERSION >= 70`. | Static evidence: `rg "persists the effective source spec\\|npm file sidecar\\|invalid registry npm version\\|path sidecar source\\|tarball sidecar source\\|DAEMON_API_VERSION" test/extension-tooling-routes-package-management.test.ts`; verification: `pnpm build`, `pnpm type-check`. | Path/tarball loop labels now name source kind. |
