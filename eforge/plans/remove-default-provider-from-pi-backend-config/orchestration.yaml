name: remove-default-provider-from-pi-backend-config
description: Remove the implicit openrouter default from Pi backend config so
  users must explicitly set pi.provider
created: 2026-03-29
compiled: 2026-03-29
mode: config-cleanup
base_branch: main
validate:
  - pnpm type-check
  - pnpm test
plans:
  - id: plan-01-remove-default-provider
    name: Remove Default Provider from Pi Backend Config
    depends_on: []
    branch: remove-default-provider-from-pi-backend-config/remove-default-provider
    build:
      - implement
      - review-cycle
    review:
      strategy: auto
      perspectives:
        - code
      maxRounds: 1
      evaluatorStrictness: standard
profile:
  extends: errand
  description: Small, self-contained changes. Single file or a few lines. Low
    risk, no architectural impact.
  compile:
    - prd-passthrough
