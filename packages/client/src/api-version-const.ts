/**
 * Browser-safe DAEMON_API_VERSION constant.
 *
 * Kept in its own module so the browser entrypoint (browser.ts) can export
 * the version number without pulling in the Node-only lockfile/fs dependencies
 * that live in api-version.ts.
 *
 * Bump `DAEMON_API_VERSION` in either of these cases:
 *
 * 1. **Breaking change** to the daemon HTTP API surface:
 *      - Renaming a route path
 *      - Removing a required request or response field
 *      - Changing the type of an existing request or response field
 *
 * 2. **First-party feature gate** — an additive change (e.g. a new optional
 *    response field) that a first-party client (Console) relies on, such that a
 *    stale daemon lacking it would produce a broken UX. Bump so clients reject
 *    stale daemons even though the contract change is technically additive.
 *
 * Adding a new **optional** response field that no first-party client gates on
 * is NOT breaking and must NOT bump the version. Removing a field, renaming a
 * route, or changing a response's required fields IS breaking and must bump.
 * (v54 is a case 2 bump: the optional recovery applied metadata is additive,
 * but Console depends on it, so stale daemons must fail version verification.)
 */
export const DAEMON_API_VERSION = 62; // v62: extends the v61 extension agent task contract for daemon-owned eforge-plan planning draft tasks — adds the optional 'sessionPlanCreationDraft' requested output section, the session-plan creation draft result variant (decision: 'ready' with sessionPlanCreationDraft), the needs-input decision variant (decision: 'needs-input' with clarificationQuestions + rationale), and bounded sectionProgress metadata (currentSection/coveredSections/remainingSections) on sanitized task metadata and section-progress events; first-party clients gate on these additions, so stale daemons must fail version verification. v61: adds first-party extension agent task routes and lifecycle events for daemon-owned eforge-plan planning draft tasks; first-party clients require task route/event support. v60: bundle workstation manifest metadata plus eforge-owned workstation frame/asset routes gate first-party clients that load extension workstations from daemon-served bundles. v59: extension contribution manifests now require consoleWorkstations arrays with manifest-carried iframe srcDoc entries and effective allowedActions for Console workstation support. v58: GET /api/recovery/sidecar returns the concise v3 recovery sidecar contract (top-level prdId/setName, verdict, operator report, boundedEvidence, generatedAt, optional applied) instead of the old full summary dump; first-party clients must reject stale daemons for this breaking response-shape change. v57: adds the queue dependency override route (`POST /api/queue/:prdId/dependencies/override`) with typed request/response contracts and client helpers, plus the durable daemon-scoped `queue:prd:dependency-overridden` audit event; first-party clients require route/event support for deterministic queue dependency override UX, so stale daemons must fail version verification. v56: accepted-success recovery preview/apply includes per-run landingAutoMerge intent and accepted-success landing audit metadata includes PR auto-merge results; monitor projections resolve accepted-success complete failed queue/run records as completed while retaining failed PRD audit files. v55: adds accepted-success recovery routes (`GET /api/recover/accept-success/preview`, `POST /api/recover/accept-success`) with typed request/response contracts (`AcceptSuccessPreviewRequest`/`AcceptSuccessPreviewResponse`, `AcceptSuccessRequest`/`AcceptSuccessResponse`) and client helpers; preview reports eligibility (`eligible`|`ineligible`|`already-applied`), the effective landing action, cleanup effects, durable audit fields, and direct skipped dependent candidates, while apply performs cleanup, landing, artifact/completion recording, dependent unblocking, and writes a durable `accepted-success` sidecar marker that makes reapply idempotent. All additions are additive, but first-party Console relies on these recovery routes, so stale daemons must fail version verification. v54: adds durable recovery applied metadata to the wire contract — optional `RecoveryAppliedMetadata` (`action`/`appliedAt`/optional `successorPrdId`/`commitSha`) carried as `RecoveryVerdictSidecar.applied`, `QueueItem.recoveryApplied` (and the matching `DaemonStreamSnapshot` queue-item schema), plus optional `ApplyRecoveryResponse.status` (`applied`|`already-applied`) and `detail`; split recovery apply is now idempotent (a repeated apply returns the original successor id rather than enqueueing a duplicate). All additions are optional, but first-party Console relies on the applied contract, so stale daemons must fail version verification. v53: adds queue priority (`POST /api/queue/:prdId/priority`) and queue removal (`DELETE /api/queue/:prdId`) route contracts with typed request/response types (`QueuePriorityRequest`/`QueuePriorityResponse`/`QueueRemoveResponse`) and client helpers; first-party clients and Console require daemon support for these queue-control routes, so stale daemons must fail version verification rather than returning route-level 404s. v52: adds first-party extension contribution manifest (`GET /api/extensions/contributions`) and action invocation (`POST /api/extensions/actions/invoke`) route contracts; stale daemons must fail version verification before clients rely on these routes. v51: `POST /api/recover/resume-build` now queues compiled resume PRDs and returns queued metadata (`kind`, `prdId`, `setName`, branches, descendants, optional profile/status/detail) instead of spawned worker `sessionId`/`pid`; stale clients expecting immediate worker identifiers must fail version verification. v50: stack landing conflict recovery adds closed `stack:landing:conflict:detected`, `stack:landing:conflict:recovery:start`, `stack:landing:conflict:recovery:complete`, and `stack:landing:conflict:recovery:failed` event variants and widens `stack:provider:command.exitCode` to allow null for provider exits without a numeric status; stale clients that validate the closed event contract must fail version verification. v49: read-only resume eligibility route added (`resumeEligibility` under `GET /api/recover/resume-eligibility`) with typed client helpers (`apiCheckResumeEligibility`/`IfRunning`), browser-safe recovery/resume fetch helpers, and `ResumeEligibilityRequest`/`ResumeEligibilityResponse`/`ResumeArtifactAvailability` wire types; first-party Console code requires daemon support for this preflight route, so stale daemons must fail version verification rather than returning a route-level 404. v48: `POST /api/recover/resume-build` accepts and validates `profile`, forwarding it to the resume worker as `--profile`; first-party clients need version agreement because older daemons silently ignored resume profile overrides. v47: read-only session plan-set routes added (`sessionPlanSetList`, `sessionPlanSetShow`, `sessionPlanSetValidate` under `/api/session-plan-set/*`) with typed client helpers (`apiSessionPlanSetList`/`Show`/`Validate` + `IfRunning` variants) and browser-safe wire types; first-party clients and Console require daemon support for these routes, so stale daemons must fail version verification rather than returning route-level 404s. v46: queue recovery analyze/apply route constants and typed helpers added so Console can repair failed upstream queue cascades through the daemon contract. v45: `build:resume:artifacts` event variant added to the closed `EforgeEvent` wire contract so resumed build details can serve recovered session artifacts from persisted events. v44: `afterQueueId` optional field added to `EnqueueRequest`; `POST /api/enqueue` validates and forwards `afterQueueId` to the enqueue worker via `--after <id>`; explicit `afterQueueId` bypasses dependency-detector output in the engine enqueue path; older daemons would silently ignore `afterQueueId`, violating deterministic handoff semantics. v43: stack sync `retry-deferred` trigger added to route validation and event schemas; new `stack:sync:skipped` event variant added; `deferred` added to closed `StackSyncResponse.outcome` union; `activeBuildSkips` and `providerCommands` fields added to `StackSyncStatusWire`; `current.outcome` made optional in daemon stream snapshot schema. v42: `plan:build:review:fix:continuation` event variant added; review-fixer now has a retry policy with turn-budget continuation support. v41: `landing:auto-merge:start`, `landing:auto-merge:complete`, `landing:auto-merge:skipped` event variants added; `landingAutoMerge` (boolean) optional field added to `EnqueueRequest`, `PlaybookRunRequest`, `BuildOptions`, `EnqueueOptions`, and PRD frontmatter (`landing_auto_merge`); `landing.pr.autoMerge` config field added (`ask|always|never`, default `ask`). v40: `gap_close:complete.passed` is now a required field on the wire event; older clients/daemons that treated it as optional will disagree on the event schema. v39: landing action vocabulary changed from full strings (merge-to-base-branch|issue-pr|leave-branch) to canonical shorthands (pr|merge|leave) in EnqueueRequest.landingAction and PlaybookRunRequest.landingAction (replacing onSuccess); daemon rejects onSuccess in request bodies with a migration error; CLI workers spawned with --landing-action instead of --on-success. v38: `landing:start` wire event removes `feature-pr-after-local-merge` workflow literal and replaces it with `feature-pr`; older clients that validated the event against the previous schema union will reject events emitted by the new daemon. v37: optional `onSuccess` field added to `PlaybookRunRequest` (per-playbook-run landing action override: 'merge-to-base-branch' | 'issue-pr' | 'leave-branch'); daemon `/api/playbook/run` validates and passes `onSuccess` to `enqueuePrd(...)` for autonomous playbooks; older daemons would silently ignore the field. v36: optional `onSuccess` field added to `EnqueueRequest` (per-build landing action override: 'merge-to-base-branch' | 'issue-pr' | 'leave-branch'); `onSuccess` persisted in PRD frontmatter so override survives queue scheduler child-process boundaries; new `landing:start`, `landing:complete`, `landing:skipped` event variants added in plan-01. v35: optional `profile` field added to playbook frontmatter wire shapes (`PlaybookListEntry`, `PlaybookData`, `PlaybookFrontmatterFields`); optional `agent_profile` field added to `SessionPlanDataWire` and `SessionPlanCreateRequest`; `/api/playbook/run` for autonomous playbooks with `profile` persists profile in queued PRD frontmatter; `/api/session-plan/create` and `/api/session-plan/create-from-playbook` propagate `agent_profile`; `/api/enqueue` validates and propagates inherited session-plan `agent_profile` to worker `--profile` arg. v34: `POST /api/playbook/run` for planning-mode playbooks now returns `{ kind: 'requires-agent'; mode: 'planning'; name; message }` (HTTP 200) instead of creating a session-plan file; `PlaybookRunResponse` discriminated union becomes `enqueued | requires-agent` (removing `planning`); `mode` field added to `GET /api/playbook/list` entries and `GET /api/playbook/show` playbook object. v33: renames route `POST /api/playbook/enqueue` → `POST /api/playbook/run` (old route returns 404); adds new route `POST /api/session-plan/create-from-playbook`; `POST /api/playbook/run` now returns a discriminated union `{ kind: 'enqueued'; id }` or `{ kind: 'planning'; session; path }` based on playbook.mode; MCP/Pi action `'enqueue'` renamed to `'run'` on eforge_playbook tool; new MCP/Pi action `'create-from-playbook'` added to eforge_session_plan tool; playbook frontmatter now requires `mode` field. v32: adds input-source/enricher provenance events (`extension:input-source:fetched`, `extension:input-source:failed`, `extension:prd-enricher:applied`, `extension:prd-enricher:failed`). v31: adds persisted `daemon:auto-build:transition` event plus optional auto-build lifecycle detail fields (`desired`, `mode`, `scheduler`, `lastTransition`, `reason`) on auto-build snapshots and heartbeat payloads. v30: adds `error_transient_transport` to the closed `AgentTerminalSubtype` union used by `plan:build:failed.terminalSubtype` and `agent:retry.subtype`. v29: adds 'pending' to RunSummary.plans[].status; /api/run-summary/:id now seeds plans from the latest planning:complete event before overlaying plan:build:start/complete/failed (falls back to build events when planning:complete is absent). v28: per-build profile override (EnqueueRequest.profile, session:profile source 'override'). v27: adds `planning:decision` event variant with `PlanningDecisionSchema` (inner discriminated union over four planning-phase decision kinds: scope-selected, build-pipeline-chosen, review-profile-chosen, plan-set-shape); introduces `emitPlanningDecision` helper in engine; extends planner to emit decisions at planning:complete; adds planning decision rendering to monitor-ui. v26: adds `plan:build:decision` event variant with `BuildDecisionSchema` (inner discriminated union over seven decision kinds); introduces `emitBuildDecision` helper in engine; adds `decisions` slice to monitor-ui reducer. v25: adds `daemon:run:upsert` daemon-scoped persisted event as authoritative source of `DaemonState.runs`; removes run synthesis from `session:start` projector; removes run termination from `session:end` projector; drops `project` functions from `enqueue:start`/`enqueue:complete`/`enqueue:failed` (replaced by `daemon:run:upsert`); enriches `queue:prd:stale` with required `prdId`+`title` fields; enriches `queue:prd:commit-failed` with required `title` field; adds `project` functions for `queue:prd:stale` and `queue:prd:commit-failed` for live queue parity. v24: rowToRunInfo now maps nullable SQL columns (session_id, completed_at, pid) to undefined rather than null — wire shape conforms to DaemonRunRecordSchema (.optional(), not .nullable()); `enqueue:complete` event gains required `planSet` field (plan-set name, currently mirrors `title`). v23: stream:hello SSE handshake primitive; removal of the v18 resync-marker mechanism on initial daemon-events connect; removal of on-connect heartbeat write; removal of on-connect heartbeat write; snapshot envelope added to stream:hello for both daemon-events and per-session streams.
