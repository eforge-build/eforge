import { access } from 'node:fs/promises';
import { API_ROUTES } from '@eforge-build/client';

export async function listPlaybooksWire(cwd: string) { const { getConfigDir } = await import('@eforge-build/engine/config'); const { listPlaybooks } = await import('@eforge-build/input'); const configDir = await getConfigDir(cwd); const result = await listPlaybooks({ configDir: configDir ?? cwd, cwd }); for (const w of result.warnings) process.stderr.write(`${w}\n`); return result; }
export async function showPlaybook(cwd: string, name: string) { const { getConfigDir } = await import('@eforge-build/engine/config'); const { loadPlaybook } = await import('@eforge-build/input'); const configDir = await getConfigDir(cwd); if (!configDir) throw Object.assign(new Error('No eforge config directory found'), { statusCode: 404 }); return loadPlaybook({ configDir, cwd, name }); }
export async function savePlaybook(cwd: string, body: any) { const { getConfigDir } = await import('@eforge-build/engine/config'); const { writePlaybook, playbookFrontmatterSchema, analyzeAcceptanceCriteria, formatAcDiagnostics } = await import('@eforge-build/input'); const fmResult = playbookFrontmatterSchema.safeParse(body.playbook.frontmatter); if (!fmResult.success) throw Object.assign(new Error('Playbook validation failed'), { statusCode: 400, body: { error: 'Playbook validation failed', errors: fmResult.error.issues.map((i: any) => (i.path.length ? `${i.path.join('.')}: ` : '') + i.message) } }); const bd = body.playbook.body; if (!bd || typeof bd.goal !== 'string' || !bd.goal) throw Object.assign(new Error('Playbook validation failed'), { statusCode: 400, body: { error: 'Playbook validation failed', errors: ['Missing required section: ## Goal'] } }); for (const f of ['outOfScope', 'acceptanceCriteria', 'plannerNotes']) if (bd[f] !== undefined && typeof bd[f] !== 'string') throw Object.assign(new Error(`Invalid field: ${f} must be a string`), { statusCode: 400 }); const playbook = { ...fmResult.data, goal: bd.goal ?? '', outOfScope: bd.outOfScope ?? '', acceptanceCriteria: bd.acceptanceCriteria ?? '', plannerNotes: bd.plannerNotes ?? '' }; if (playbook.acceptanceCriteria) { const ac = analyzeAcceptanceCriteria(playbook.acceptanceCriteria); if (!ac.valid) throw Object.assign(new Error(`Playbook acceptance criteria quality gate failed:\n${formatAcDiagnostics(ac.diagnostics)}`), { statusCode: 400 }); } const configDir = await getConfigDir(cwd); const result = await writePlaybook({ configDir: configDir ?? cwd, cwd, scope: body.scope, playbook }); return { path: result.path }; }
export async function runPlaybook(cwd: string, options: { queueDir?: string }, notify: () => void, body: any) { const { getConfigDir, getConventionalConfigDir, loadConfig, loadProfile } = await import('@eforge-build/engine/config'); const input = await import('@eforge-build/input'); const configDir = await getConfigDir(cwd); if (!configDir) throw Object.assign(new Error('No eforge config directory found'), { statusCode: 404 }); const { playbook } = await input.loadPlaybook({ configDir, cwd, name: body.name }); if (body.landingAutoMerge === true) { const { config } = await loadConfig(cwd); const effective = body.landingAction ?? (config as any).landing?.action ?? 'merge'; if (effective !== 'pr') throw Object.assign(new Error(`Invalid field: landingAutoMerge can only be true when the effective landing action is 'pr' (got '${effective}')`), { statusCode: 400 }); if ((config as any).landing?.pr?.autoMerge === 'never') throw Object.assign(new Error("landingAutoMerge: true is not allowed when landing.pr.autoMerge is 'never' in project config"), { statusCode: 400 }); } if (playbook.mode === 'planning') return { kind: 'requires-agent', mode: 'planning', name: body.name, message: `Playbook "${body.name}" is planning-mode. Use /eforge:playbook run ${body.name} to start an interactive planning session.` }; const plan = input.playbookToBuildSource(playbook); const ac = input.analyzeAcceptanceCriteriaInBody(plan.source); if (ac && !ac.valid) throw Object.assign(new Error(`Playbook acceptance criteria quality gate failed:\n${input.formatAcDiagnostics(ac.diagnostics)}`), { statusCode: 400 }); const { deriveAcceptanceCriteriaInventoryFromPrdBody } = await import('@eforge-build/engine/validation/acceptance-criteria-inventory'); let acceptanceCriteriaInventory; try { acceptanceCriteriaInventory = deriveAcceptanceCriteriaInventoryFromPrdBody(plan.source); } catch (e) { throw Object.assign(new Error(e instanceof Error ? e.message : String(e)), { statusCode: 400 }); } if (playbook.profile) { const cfgDir = await getConfigDir(cwd) ?? getConventionalConfigDir(cwd); if (!await loadProfile(cfgDir, playbook.profile, cwd)) throw Object.assign(new Error(`Playbook profile '${playbook.profile}' not found`), { statusCode: 400 }); }
  const { enqueuePrd, inferTitle, classifyAfterQueueId } = await import('@eforge-build/engine/prd-queue'); const queueDir = options.queueDir ?? '.eforge/queue'; let depends_on: string[] | undefined; let intoWaiting = false; if (body.afterQueueId) { try { const p = await classifyAfterQueueId(body.afterQueueId, queueDir, cwd); depends_on = p.dependsOn; intoWaiting = p.intoWaiting; } catch (e) { throw Object.assign(new Error(e instanceof Error ? e.message : `Invalid afterQueueId: ${body.afterQueueId}`), { statusCode: 404 }); } } const result = await enqueuePrd({ body: plan.source, title: inferTitle(plan.source, plan.name), queueDir, cwd, depends_on, intoWaiting, postMerge: plan.postMerge, profile: plan.profile, acceptanceCriteriaInventory, ...(body.landingAction !== undefined && { landingAction: body.landingAction }), ...(body.landingAutoMerge !== undefined && { landingAutoMerge: body.landingAutoMerge }) }); notify(); return { kind: 'enqueued', id: result.id }; }
export async function movePlaybookWire(cwd: string, name: string, promote: boolean) { const { getConfigDir } = await import('@eforge-build/engine/config'); const { movePlaybook } = await import('@eforge-build/input'); const configDir = await getConfigDir(cwd); if (!configDir) throw Object.assign(new Error('No eforge config directory found'), { statusCode: 404 }); const result = await movePlaybook({ configDir, cwd, name, fromScope: promote ? 'project-local' : 'project-team', toScope: promote ? 'project-team' : 'project-local' }); return { path: result.path }; }
export async function validatePlaybookRaw(raw: string) { const { validatePlaybook } = await import('@eforge-build/input'); const result = validatePlaybook(raw); return result.ok ? { ok: true } : { ok: false, errors: result.errors }; }
export async function copyPlaybookWire(cwd: string, body: any) { const { getConfigDir } = await import('@eforge-build/engine/config'); const { copyPlaybookToScope } = await import('@eforge-build/input'); const configDir = await getConfigDir(cwd); if (!configDir) throw Object.assign(new Error('No eforge config directory found'), { statusCode: 404 }); const r = await copyPlaybookToScope({ configDir, cwd, name: body.name, targetScope: body.targetScope }); return { sourcePath: r.sourcePath, targetPath: r.targetPath, targetScope: r.targetScope }; }
export async function createFromPlaybook(cwd: string, body: any) { const { getConfigDir } = await import('@eforge-build/engine/config'); const { loadPlaybook, createSessionPlanFromPlaybookSeed, writeSessionPlan, resolveSessionPlanPath } = await import('@eforge-build/input'); const configDir = await getConfigDir(cwd); if (!configDir) throw Object.assign(new Error('No eforge config directory found'), { statusCode: 404 }); const { playbook } = await loadPlaybook({ configDir, cwd, name: body.playbook_name }); if (playbook.mode !== 'planning') throw Object.assign(new Error(`Playbook "${body.playbook_name}" is autonomous — use POST ${API_ROUTES.playbookRun} to enqueue it`), { statusCode: 400 }); const plan = createSessionPlanFromPlaybookSeed({ playbook, session: typeof body.session === 'string' ? body.session : undefined, topic: typeof body.topic === 'string' ? body.topic : undefined }); const planPath = resolveSessionPlanPath({ cwd, session: plan.session }); let exists = false; try { await access(planPath); exists = true; } catch (e) { if ((e as NodeJS.ErrnoException).code !== 'ENOENT') throw e; } if (exists) throw Object.assign(new Error(`Session plan already exists: ${plan.session}`), { statusCode: 409 }); await writeSessionPlan({ cwd, plan }); return { session: plan.session, path: planPath }; }
