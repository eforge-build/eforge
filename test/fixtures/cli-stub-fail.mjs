#!/usr/bin/env node
// Minimal CLI stub that immediately exits with code 1 (failed).
// Used by watch-queue.test.ts to ensure subprocess-based queue tests
// complete quickly without spinning up a real eforge build.
process.exit(1);
