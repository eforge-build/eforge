import { describe, it } from 'vitest';

describe('stack runtime landing', () => {
  it('is covered by the focused stack runtime landing suites', () => {
    // Coverage was split into stack-runtime-landing-*.test.ts.
  });
});
